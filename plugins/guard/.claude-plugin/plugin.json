{
  "name": "guard",
  "version": "0.1.0",
  "description": "Helper skill for the `guard` CLI (https://github.com/florianbuetow/guard), a file-permission tool that protects files from accidental modification by AI agents via immutable flags and root ownership. Composes guard commands for five intents — init, create-collection, remove-collection, clear-all, info — running registry operations directly and printing `sudo guard` commands for operations that need root.",
  "author": {
    "name": "Florian Buetow",
    "email": "2320560+florianbuetow@users.noreply.github.com"
  },
  "license": "MIT",
  "keywords": ["guard", "file-permissions", "ai-safety", "immutable", "schg"]
}
